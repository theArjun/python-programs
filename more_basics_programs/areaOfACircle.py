# Pi is taken as 22.0 because the operation between integer and integer is
# always integer, so to make it float we take it as 22.0


radius = float(input("Enter radius of circle : "))
print('Area of circle is : ', (22.0/7)*radius*radius)
